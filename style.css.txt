@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700;900&family=M+PLUS+Rounded+1c:wght@400;700;800&display=swap');
 
/* ===== VARIABLES ===== */
:root {
  --green-dark:  #1a6b3a;
  --green-main:  #2d9b5a;
  --green-light: #4cbb7a;
  --green-pale:  #e8f7ef;
  --green-pale2: #c8ecda;
  --yellow:  #f9d84b;
  --orange:  #f4883d;
  --blue:    #3a87d4;
  --text-dark:  #1a2c1e;
  --text-mid:   #3d5c44;
  --text-light: #6b8f74;
  --white: #ffffff;
  --shadow:       0 8px 32px rgba(45, 155, 90, 0.12);
  --shadow-hover: 0 16px 40px rgba(45, 155, 90, 0.20);
  --radius: 16px;
  --radius-lg: 20px;
}
 
/* ===== RESET ===== */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
img  { display: block; max-width: 100%; }
ul   { list-style: none; }
 
body {
  font-family: 'Noto Sans JP', sans-serif;
  background: #f4fbf6;
  color: var(--text-dark);
  line-height: 1.7;
}
 
/* ===== COMMON SECTION ===== */
section       { padding: 72px 32px; }
.container    { max-width: 880px; margin: 0 auto; }
 
.section-label {
  display: inline-block;
  background: var(--green-pale2);
  color: var(--green-dark);
  font-weight: 700;
  font-size: 0.78rem;
  letter-spacing: 0.1em;
  padding: 5px 16px;
  border-radius: 999px;
  margin-bottom: 12px;
}
 
h2 {
  font-family: 'M PLUS Rounded 1c', sans-serif;
  font-size: clamp(1.5rem, 4vw, 2rem);
  font-weight: 800;
  color: var(--green-dark);
  margin-bottom: 32px;
  line-height: 1.3;
}
h2 span { color: var(--green-light); }
 
/* ===== HERO ===== */
.hero {
  background: linear-gradient(135deg, var(--green-dark) 0%, var(--green-main) 60%, var(--green-light) 100%);
  color: white;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  position: relative;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute;
  top: -80px; right: -80px;
  width: 400px; height: 400px;
  background: rgba(255,255,255,0.06);
  border-radius: 50%;
}
.hero::after {
  content: '';
  position: absolute;
  bottom: -120px; left: -60px;
  width: 500px; height: 500px;
  background: rgba(255,255,255,0.04);
  border-radius: 50%;
}
.hero-inner {
  max-width: 860px;
  margin: 0 auto;
  padding: 80px 32px 60px;
  position: relative;
  z-index: 1;
}
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(255,255,255,0.18);
  border: 1px solid rgba(255,255,255,0.3);
  border-radius: 999px;
  padding: 8px 20px;
  font-size: 13px;
  font-weight: 700;
  letter-spacing: 0.06em;
  margin-bottom: 28px;
  backdrop-filter: blur(8px);
}
.hero-badge span { font-size: 16px; }
.hero h1 {
  font-family: 'M PLUS Rounded 1c', sans-serif;
  font-size: clamp(2rem, 5vw, 3.2rem);
  font-weight: 800;
  line-height: 1.25;
  margin-bottom: 20px;
  text-shadow: 0 2px 16px rgba(0,0,0,0.15);
}
.hero h1 em { font-style: normal; color: var(--yellow); }
.hero-sub {
  font-size: clamp(1rem, 2.5vw, 1.2rem);
  opacity: 0.88;
  margin-bottom: 16px;
}
.hero-catch {
  background: rgba(255,255,255,0.12);
  border-left: 4px solid var(--yellow);
  border-radius: 0 12px 12px 0;
  padding: 16px 24px;
  margin: 32px 0;
  font-size: 0.97rem;
  line-height: 1.8;
}
.hero-cta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: var(--yellow);
  color: var(--green-dark);
  font-weight: 900;
  font-size: 1.05rem;
  padding: 16px 36px;
  border-radius: 999px;
  text-decoration: none;
  box-shadow: 0 4px 20px rgba(249,216,75,0.4);
  transition: transform 0.2s, box-shadow 0.2s;
}
.hero-cta:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 28px rgba(249,216,75,0.5);
}
.hero-deadline {
  margin-top: 20px;
  font-size: 0.88rem;
  opacity: 0.75;
}
 
/* WAVE */
.wave {
  display: block;
  width: 100%;
  overflow: hidden;
  line-height: 0;
  margin-top: -2px;
}
 
/* ===== INTRO ===== */
.intro { background: white; }
.intro-lead { font-size: 1.08rem; color: var(--text-mid); line-height: 1.9; }
.intro-highlight {
  display: inline;
  background: linear-gradient(transparent 60%, rgba(76,187,122,0.3) 60%);
  font-weight: 700;
}
 
/* ===== SCHEDULE ===== */
.schedule { background: var(--green-pale); }
.timeline { display: flex; flex-direction: column; gap: 0; }
.timeline-item { display: flex; gap: 0; position: relative; }
.timeline-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 64px;
  flex-shrink: 0;
}
.timeline-dot {
  width: 44px; height: 44px;
  border-radius: 50%;
  background: var(--green-main);
  color: white;
  font-weight: 900;
  font-size: 0.85rem;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 4px 12px rgba(45,155,90,0.3);
  position: relative;
  z-index: 1;
}
.timeline-dot.last { background: var(--orange); }
.timeline-line {
  width: 2px;
  flex: 1;
  background: var(--green-pale2);
  min-height: 32px;
}
.timeline-item:last-child .timeline-line { display: none; }
.timeline-content {
  background: white;
  border-radius: var(--radius);
  padding: 20px 24px;
  margin-bottom: 16px;
  margin-left: 12px;
  box-shadow: var(--shadow);
  flex: 1;
  border-left: 4px solid var(--green-light);
  transition: transform 0.2s;
}
.timeline-content:hover { transform: translateX(4px); }
.timeline-content.last { border-color: var(--orange); }
.timeline-date { font-size: 0.82rem; font-weight: 700; color: var(--green-main); margin-bottom: 4px; }
.timeline-date.last { color: var(--orange); }
.timeline-title { font-weight: 700; font-size: 1rem; color: var(--text-dark); margin-bottom: 4px; }
.timeline-desc { font-size: 0.88rem; color: var(--text-light); }
 
/* ===== PLACES（訪問予定先）===== */
.places { background: white; }
.places-intro {
  color: var(--text-light);
  font-size: 0.97rem;
  margin-bottom: 40px;
}
.place-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 24px;
}
.place-card {
  border-radius: var(--radius-lg);
  overflow: hidden;
  background: #fff;
  border: 1.5px solid var(--green-pale2);
  box-shadow: var(--shadow);
  transition: transform 0.22s, box-shadow 0.22s;
}
.place-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-hover);
}
 
/* 写真プレースホルダー */
.place-photo {
  width: 100%;
  aspect-ratio: 4 / 3;
  overflow: hidden;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}
.place-photo img {
  width: 100%; height: 100%;
  object-fit: cover;
  transition: transform 0.35s;
}
.place-card:hover .place-photo img { transform: scale(1.06); }
 
/* 写真がない場合のフォールバック（絵文字 + グラデ） */
.place-photo-bg {
  width: 100%; height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 3.5rem;
}
.place-photo-bg.welfare  { background: linear-gradient(135deg,#e8f7ef,#c8ecda); }
.place-photo-bg.craft    { background: linear-gradient(135deg,#fff8e8,#ffeec8); }
.place-photo-bg.npo      { background: linear-gradient(135deg,#edf6ff,#d6eaff); }
.place-photo-bg.cafe     { background: linear-gradient(135deg,#fdf2f8,#f5d5ea); }
 
.place-info { padding: 20px; }
.place-area {
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--green-main);
  letter-spacing: 0.06em;
  margin-bottom: 6px;
}
.place-name {
  font-family: 'M PLUS Rounded 1c', sans-serif;
  font-weight: 800;
  font-size: 1rem;
  color: var(--text-dark);
  margin-bottom: 12px;
  line-height: 1.4;
}
.place-features {
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.place-features li {
  font-size: 0.82rem;
  color: var(--text-mid);
  padding-left: 16px;
  position: relative;
}
.place-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--green-light);
  font-size: 0.7rem;
  top: 2px;
}
 
/* ===== CATEGORY ===== */
.category { background: var(--green-pale); }
.category-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 24px;
}
.category-card {
  border-radius: var(--radius-lg);
  padding: 32px 24px;
  transition: transform 0.2s, box-shadow 0.2s;
}
.category-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-hover);
}
.category-card.junior    { background: linear-gradient(135deg,#e8f7ef,#d0eddf); border: 2px solid var(--green-pale2); }
.category-card.university { background: linear-gradient(135deg,#edf6ff,#d6eaff); border: 2px solid #c0d9f5; }
.category-card.mentor    { background: linear-gradient(135deg,#fff8e8,#ffeec8); border: 2px solid #f5dfa0; }
.category-icon { font-size: 2.4rem; margin-bottom: 12px; }
.category-tag {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 700;
  padding: 3px 12px;
  border-radius: 999px;
  margin-bottom: 12px;
}
.junior .category-tag    { background: var(--green-main); color: white; }
.university .category-tag { background: var(--blue); color: white; }
.mentor .category-tag    { background: var(--orange); color: white; }
.category-name {
  font-family: 'M PLUS Rounded 1c', sans-serif;
  font-weight: 800;
  font-size: 1.05rem;
  color: var(--text-dark);
  margin-bottom: 8px;
}
.category-quota { font-size: 0.82rem; font-weight: 700; color: var(--text-light); margin-bottom: 12px; }
.category-desc  { font-size: 0.88rem; color: var(--text-mid); line-height: 1.7; }
 
/* ===== MESSAGE ===== */
.message {
  background: linear-gradient(135deg, var(--green-dark), var(--green-main));
  color: white;
  text-align: center;
}
.message .section-label { background: rgba(255,255,255,0.2); color: white; }
.message h2 { color: var(--yellow); }
.message-body {
  max-width: 620px;
  margin: 0 auto;
  font-size: 1.05rem;
  line-height: 1.9;
  opacity: 0.92;
}
.message-body strong { color: var(--yellow); font-weight: 700; }
 
/* ===== TEAM（運営メンバー）===== */
.team { background: var(--green-pale); }
.team-intro { color: var(--text-light); font-size: 0.97rem; margin-bottom: 40px; }
 
.member-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 28px;
}
.member-card {
  background: white;
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow);
  border: 1.5px solid var(--green-pale2);
  transition: transform 0.22s, box-shadow 0.22s;
  text-align: center;
}
.member-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-hover);
}
 
/* 写真エリア */
.member-photo {
  width: 100%;
  aspect-ratio: 1 / 1;
  overflow: hidden;
  position: relative;
}
.member-photo img {
  width: 100%; height: 100%;
  object-fit: cover;
  object-position: top center;
}
 
/* 写真がない場合のアバター */
.member-avatar {
  width: 100%; height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 3rem;
  font-weight: 900;
  color: white;
  letter-spacing: -1px;
}
.member-avatar.green  { background: linear-gradient(135deg,#2d9b5a,#4cbb7a); }
.member-avatar.teal   { background: linear-gradient(135deg,#1a8fa0,#2ec4b6); }
.member-avatar.purple { background: linear-gradient(135deg,#7c3aed,#a855f7); }
.member-avatar.orange { background: linear-gradient(135deg,#f4883d,#f9d84b); }
 
.member-info { padding: 20px 16px 24px; }
.member-role {
  display: inline-block;
  font-size: 0.72rem;
  font-weight: 700;
  background: var(--green-pale2);
  color: var(--green-dark);
  padding: 3px 12px;
  border-radius: 999px;
  letter-spacing: 0.06em;
  margin-bottom: 10px;
}
.member-name {
  font-family: 'M PLUS Rounded 1c', sans-serif;
  font-weight: 800;
  font-size: 1.08rem;
  color: var(--text-dark);
  margin-bottom: 4px;
}
.member-univ {
  font-size: 0.78rem;
  color: var(--text-light);
  margin-bottom: 12px;
}
.member-bio {
  font-size: 0.83rem;
  color: var(--text-mid);
  line-height: 1.75;
  text-align: left;
}
 
/* ===== INFO ===== */
.info { background: white; }
.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
  margin-bottom: 40px;
}
.info-item {
  background: var(--green-pale);
  border-radius: var(--radius);
  padding: 20px;
  box-shadow: var(--shadow);
}
.info-item.accent { border-left: 4px solid var(--orange); }
.info-label { font-size: 0.78rem; font-weight: 700; color: var(--green-main); margin-bottom: 6px; letter-spacing: 0.06em; }
.info-item.accent .info-label { color: var(--orange); }
.info-value { font-weight: 700; font-size: 0.97rem; color: var(--text-dark); }
.info-item.accent .info-value { color: var(--orange); }
.info-value small { font-weight: 400; color: var(--text-light); }
 
/* ===== CTA ===== */
.cta-section { background: var(--green-pale); text-align: center; padding: 80px 32px; }
.cta-section h2    { margin-bottom: 16px; }
.cta-section > .container > p { color: var(--text-light); margin-bottom: 36px; font-size: 0.97rem; }
.btn-cta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: linear-gradient(135deg, var(--green-main), var(--green-light));
  color: white;
  font-weight: 900;
  font-size: 1.1rem;
  padding: 18px 48px;
  border-radius: 999px;
  text-decoration: none;
  box-shadow: 0 6px 24px rgba(45,155,90,0.35);
  transition: transform 0.2s, box-shadow 0.2s;
}
.btn-cta:hover {
  transform: translateY(-3px);
  box-shadow: 0 12px 32px rgba(45,155,90,0.45);
}
.cta-deadline { margin-top: 20px; font-size: 0.88rem; color: var(--text-light); }
.cta-deadline strong { color: var(--orange); font-weight: 700; }
 
/* ===== FOOTER ===== */
footer {
  background: var(--text-dark);
  color: rgba(255,255,255,0.55);
  text-align: center;
  padding: 28px 32px;
  font-size: 0.82rem;
}
footer strong { color: rgba(255,255,255,0.85); }
 
/* ===== ANIMATIONS ===== */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}
.hero-inner > * { animation: fadeUp 0.7s ease both; }
.hero-inner > *:nth-child(1) { animation-delay: 0.10s; }
.hero-inner > *:nth-child(2) { animation-delay: 0.25s; }
.hero-inner > *:nth-child(3) { animation-delay: 0.35s; }
.hero-inner > *:nth-child(4) { animation-delay: 0.45s; }
.hero-inner > *:nth-child(5) { animation-delay: 0.55s; }
.hero-inner > *:nth-child(6) { animation-delay: 0.65s; }
 
/* ===== RESPONSIVE ===== */
@media (max-width: 700px) {
  section { padding: 56px 20px; }
  .hero-inner { padding: 60px 20px 48px; }
  .category-grid,
  .member-grid,
  .place-grid { grid-template-columns: 1fr; }
  .info-grid  { grid-template-columns: 1fr 1fr; }
  .btn-cta    { padding: 16px 32px; font-size: 1rem; }
}
@media (max-width: 400px) {
  .info-grid  { grid-template-columns: 1fr; }
}